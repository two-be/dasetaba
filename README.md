## Dasetaba

```fish
Description:
  Sample app for System.CommandLine

Usage:
  Dasetaba [options]

Options:
  --create           Ensures that the database for the context exists.
  --snake-column
  --source <source>
  --dest <dest>
  --version          Show version information
  -?, -h, --help     Show help and usage information
```